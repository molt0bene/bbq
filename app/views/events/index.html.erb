<h1><%= I18n.t('forms.headers.events.index') %></h1>

<div class="buffer-both">
  <%= link_to "#{I18n.t('forms.buttons.create_event')}", new_event_path, class: 'btn btn-success' %>
</div>

<div class="row">
  <%= render partial: 'events/event', collection: @events %>
</div>

